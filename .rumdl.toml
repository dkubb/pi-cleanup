# rumdl configuration — maximum strictness

[global]
# Enable opt-in rules for maximum coverage
extend-enable = ["MD060", "MD063"]

exclude = [
    ".git",
    "node_modules",
]

respect-gitignore = true

[MD013]
line-length = 100
code-blocks = false
tables = false
headings = true
